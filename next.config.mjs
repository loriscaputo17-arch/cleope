/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["firebasestorage.googleapis.com", "www.google.com", "images.xceed.me"],
  },
  reactStrictMode: true,
};

export default nextConfig;


